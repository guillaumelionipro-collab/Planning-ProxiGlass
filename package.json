{
  "name": "proxiglass-pwa-agenda",
  "version": "1.0.0",
  "private": true,
  "scripts": {
  "dev": "vite --open",
  "dev:lan": "vite --host",
  "build": "tsc -b && vite build",
  "preview": "vite preview && start http://localhost:4173",
  "preview:lan": "vite preview --host 0.0.0.0 --port 4173",
  "start": "npm run build && npm run preview"
}

  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.5",
    "@types/react-dom": "^18.3.0",
    "typescript": "^5.6.2",
    "vite": "^5.4.0",
    "@vitejs/plugin-react": "^4.3.1"
  }
}
